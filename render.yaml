services:
  - type: worker
    name: freefire-controller
    runtime: python
    region: frankfurt
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python telegram_controller.py
    envVars:
      - key: TOKEN
        sync: false
    healthCheckPath: /
    autoDeploy: true
